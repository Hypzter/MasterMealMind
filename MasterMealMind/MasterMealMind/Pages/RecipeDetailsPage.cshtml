@page
@model MasterMealMind.Web.Pages.RecipeDetailsPageModel
@{

}
@if (Model.Recipe != null)
{
	<h1>@Model.Recipe.Title</h1>
	<table class="table table-striped">
		<thead>
			<tr>
				<th scope="col">Recept</th>
				<th scope="col"></th>
			</tr>
		</thead>
		<tbody>
@* 			@foreach (var recipe in Model.Recipe.IngredientGroups)
			{
				<tr>
					<td>
						<h3>@recipe.GroupName</h3>
					</td>
				</tr>
				foreach (var ingredient in recipe.Ingredients)
				{
					<tr>
						<td>
							<p>@ingredient.Text</p>
						</td>
					</tr>

				}
			} *@

			@foreach(var steps in Model.Recipe.CookingSteps)
			{
				<tr>
					<td>
						<p>@Html.Raw(steps)</p>
					</td>
				</tr>
			}
		</tbody>
	</table>
}